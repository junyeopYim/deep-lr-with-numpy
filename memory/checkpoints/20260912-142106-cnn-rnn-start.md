# Checkpoint — 04 CNN / 05 RNN·LSTM — 2026-09-12

## The story so far
기존 00–03은 그대로 유지합니다. 사용자의 수정된 방향에 따라 04 CNN·이미지 다중분류,
05 RNN·LSTM·시퀀스 분류를 만들고 각각 새 커널에서 실행합니다. 현재 구현 준비 단계입니다.

## Decided
D-001: 베이스 아키텍처와 학습 목적 중심, CNN·RNN·Transformer와 기본 지도학습부터.

## Waiting on the user
현재 제작에 필요한 추가 결정은 없습니다.

## Next first action
`src/nn/convolution.py`에 합성곱·풀링 계산을 작성하고 노트북 본문에 같은 핵심 계산을 배치합니다.

## Tried
기존 04 정규화 / 05 최적화 기획은 사용자의 방향 수정으로 중단했습니다.
