# 2026-09-13 · 코드 주석 규칙(WRITING.md 7절) 적용: 05 – 10

D-013의 시범(00·01b) 뒤 HANDOFF 절차대로 아키텍처 노트북 여섯 개의 코드 셀을 재작성했습니다. 구현·수식·검산·실험 설정은 그대로 두고,
출력이 같은 가독성 재작성만 했습니다. 재작성 전 노트북에 저장돼 있던 출력(stream 문장, PNG md5)과 새 커널 실행 결과를 비교했습니다.
아래 실행 시간은 16개를 한 번에 재실행한 마지막 확인의 값입니다.

| 노트북 | 셀 | 그림 | 오류 | 실행 | 그림 바이트 동일 | 출력 문장 차이 |
|---|---|---|---|---|---|---|
| 05 | 72 → 78 | 9 | 0 | 3.4초 | 9 / 9 | 없음 (53줄 동일) |
| 06 | 53 → 58 | 7 | 0 | 2.6초 | 7 / 7 | 없음 (22줄 동일) |
| 07 | 64 → 73 | 8 | 0 | 3.3초 | 8 / 8 | 검산만 있던 3절 유지량 셀에 통과 print 1줄 추가. 나머지 28줄 동일 |
| 08 | 63 → 68 | 8 | 0 | 2.5초 | 8 / 8 | 없음 (31줄 동일) |
| 09 | 61 → 78 | 7 | 0 | 2.8초 | 7 / 7 | 검산만 있던 5절 데이터 분할 셀에 통과 print 1줄 추가. 나머지 51줄 동일 |
| 10 | 64 → 70 | 7 | 0 | 2.2초 | 7 / 7 | 없음 (39줄 동일) |

- verify_exercises: 여섯 노트북 모두 4/4 통과. check_code_comments: 여섯 노트북 통과. pytest 22 통과.
- 셀 증가(25줄 초과 셀을 함수별 셀로): 05 6개(35줄 → conv_backward / check_conv_backward, 36줄 → make_digits / ce_loss / cnn_forward / cnn_backward, periodic_conv·init_cnn_filters 정의/실행 분리),
  06 5개(32줄 → rnn_backward / check_rnn_backward, 40줄 → order_data / ce_loss / init_model / model_loss / adam_step),
  07 9개(27·44·53·48줄 셀 넷 → 13개 셀: lstm_step_backward·검사기, gru_step·backward·init_gates·검사기, gated_forward·backward·검사기, memory_data·loss·adam·train),
  08 5개(26줄 → softmax / attention_backward / 검사기, 44줄 → retrieval_data / ce_loss / retrieval_loss / adam_step / init_retrieval),
  09 17개(24·45·27·46·54·36줄 셀 여섯 → 22개 셀: layernorm, MHA 5개, 블록 5개, transformer 6개, 학습 3개),
  10 6개(35줄 → gnn_layer_forward·backward / 검사기, 28줄 → adjacency / graph_forward / graph_backward / init_graph, 24줄 → graph_data / ce_loss / adam_step).
- 재작성 종류: docstring(def 05 27, 06 19, 07 28, 08 21, 09 35, 10 25개), `# 쓰는 것:` 출처 줄(19·13·21·13·23·18셀), 코드 상자(8·7·7·7·6·7개),
  `lambda` → `def`(5·2·2·2·4·3), 한 줄의 두 문장 분리(0·3·4·7·2·5곳), `assert a and b` 분리, assert 메시지와 `# 검산:` 주석 전부, 07 backward의 줄마다 수식 항 주석.
- 10의 오답 검출 셀에서 예외 변수 `error`가 검사기 `check_gnn_layer` 안의 지역 이름과 겹쳐 정적 점검이 "멀리서 온 이름"으로 오탐해 `failure`로 바꿨습니다.
  그 셀이 출력하는 assert 메시지 튜플 `(mode, name, error)`는 그대로라 출력이 같습니다.
- 비교 절차: 재작성 전 사본과 출력 스냅샷(`scripts/compare_notebook_outputs.py snapshot`)을 세션 scratchpad와 `figures/check/snapshots/`에 두고,
  `scripts/nbedit.py`로 고유 문자열이 가리키는 셀을 통째로 바꾼 뒤 `check_code_comments.py` → `check_notebook.py`(새 커널) → `verify_exercises.py` →
  `compare_notebook_outputs.py compare`(stream 텍스트는 시간 수치를 마스킹한 뒤 diff, 그림은 PNG md5). `run_check` 셀의 `False` 표시와 `src/utils/*_plots.py`는 손대지 않았습니다.
- 같은 날 이어서 D-014로 `run_check`의 반환값을 없애 검사 셀의 `False` 표시를 정리했습니다. 그 재실행·비교 결과는 `results/foundation-validation/2026-09-13-run-check-no-return.md`에 있습니다.
