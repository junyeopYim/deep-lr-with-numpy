# 2026-09-13 · 코드 주석 규칙(WRITING.md 7절) 적용: 11 – 15

D-013의 시범(00·01b) 뒤 HANDOFF 절차대로 연결 노트북 다섯 개의 코드 셀을 재작성했습니다. 구현·수식·검산·실험 설정은 그대로 두고,
출력이 같은 가독성 재작성만 했습니다. 재작성 전 노트북에 저장돼 있던 출력(stream 문장, PNG md5)과 새 커널 실행 결과를 비교했습니다.
아래 실행 시간은 16개를 한 번에 재실행한 마지막 확인의 값입니다.

| 노트북 | 셀 | 그림 | 오류 | 실행 | 그림 바이트 동일 | 출력 문장 차이 |
|---|---|---|---|---|---|---|
| 11 | 74 (변동 없음) | 11 | 0 | 4.0초 | 11 / 11 | 없음 (38줄 동일) |
| 12 | 77 (변동 없음) | 11 | 0 | 3.4초 | 11 / 11 | 없음 (33줄 동일) |
| 13 | 67 → 70 | 10 | 0 | 7.2초 | 10 / 10 | 없음 (29줄 동일) |
| 14 | 69 (변동 없음) | 8 | 0 | 3.0초 | 8 / 8 | 없음 (31줄 동일) |
| 15 | 71 → 72 | 8 | 0 | 3.1초 | 8 / 8 | 없음 (40줄 동일) |

- verify_exercises: 다섯 노트북 모두 4/4 통과. check_code_comments: 다섯 노트북 통과. pytest 22 통과.
- 셀 증가: 13 3개(as_node 분리, 31줄 연산 셀 → add·multiply / matmul·tanh·total_sum, graph 함수 / manual_mlp 분리), 15 1개(29줄 셀 → sigmoid·logistic_objective / train_logistic).
  11·12·14는 정의와 실행이 이미 나뉘어 있어 셀 수가 그대로입니다.
- 13의 `Node` 클래스(51줄)와 `check_matmul`(37줄)은 정의 하나뿐이라 셀을 나눌 수 없어 메서드 docstring과 단계 번호(1–4) 주석을 두고,
  `scripts/check_code_comments.py`가 class 하나 또는 def 하나만 있는 정의 셀을 25줄 제한에서 빼도록 고쳤습니다(WRITING.md 7절 5번 규칙에 예외 한 문장 추가). 사용자 확인 대상입니다.
- 재작성 종류: docstring(def 11 18, 12 22, 13 41(중첩 back 포함), 14 19, 15 19개), `# 쓰는 것:` 출처 줄(18·17·16·16·16셀), 코드 상자(7·8·8·7·8개),
  `lambda` → `def`(4·7·2·3·2), 한 줄의 두 문장 분리(8·6·6·6·7곳), `assert a and b` 분리, assert 메시지와 `# 검산:` 주석 전부.
- 비교 절차: 재작성 전 사본과 출력 스냅샷(`scripts/compare_notebook_outputs.py snapshot`)을 세션 scratchpad와 `figures/check/snapshots/`에 두고,
  `scripts/nbedit.py`로 고유 문자열이 가리키는 셀을 통째로 바꾼 뒤 `check_code_comments.py` → `check_notebook.py`(새 커널) → `verify_exercises.py` →
  `compare_notebook_outputs.py compare`(stream 텍스트는 시간 수치를 마스킹한 뒤 diff, 그림은 PNG md5). `run_check` 셀의 `False` 표시와 `src/utils/*_plots.py`는 손대지 않았습니다.
- 같은 날 이어서 D-014로 `run_check`의 반환값을 없애 검사 셀의 `False` 표시를 정리했습니다. 그 재실행·비교 결과는 `results/foundation-validation/2026-09-13-run-check-no-return.md`에 있습니다.
