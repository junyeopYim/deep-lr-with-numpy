# 프로젝트 작업 지침

## 학습 방향 (2026-09-12 사용자 확정)

- 기존 프로젝트의 번호·폴더·구현 유지보다 사용자에게 맞는 학습 방식을 우선합니다.
- 목표는 개념을 수식으로 구성하고 배열 연산과 코드로 옮기며, 이해를 검산하는 것입니다.
- 행렬·미분·확률은 코드와 함께 다시 확인합니다. 작은 완성 예제를 이해한 뒤 직접 바꾸는 문제를 풉니다.
- 각 기호의 shape와 축의 의미를 명시하고, 손계산 → 반복문 → 벡터화의 연결을 보여 줍니다.
- 아키텍처, 학습 목적, optimizer, 학습 후 사용·생성 과정을 구분하여 연결합니다. 전체 순서는 `docs/CURRICULUM.md`를 따릅니다.
- 새 노트북은 마지막에 설명·수식·구현·변형 검산을 각각 확인하는 준비도 질문을 둡니다.

## 학습용 노트북 작성

- 각 절은 수식·설명 → 핵심 구현 → 검산 → 그림·해석 순서로 구성합니다. 핵심 함수 정의와 실행 예제·검산은 별도 셀로 나눕니다.
- 학습 대상인 모델, 활성화 함수, 손실, 역전파, 가중치 갱신과 실험 코드는 노트북 본문에 남깁니다. 그림에 필요한 은닉층 출력, 결정 경계 식, 도함수, 손실·곡률, 예측 확률도 본문에서 계산합니다.
- 긴 시각화 코드는 `src/utils/*_plots.py` 보조 파일로 분리하고, 노트북에는 계산된 결과를 전달하는 짧은 호출을 별도 셀에 둡니다. 보조 함수는 전역 노트북 변수에 의존하지 않고 인자로 받은 데이터를 그립니다. 모델 학습이나 핵심 수학 계산을 그림 함수 안에 숨기지 않습니다.
- 색상·폰트·범례·주석 위치·서브플롯 배치는 보조 파일에서 관리합니다. 짧고 읽기 쉬운 그림 코드는 필요에 따라 본문에 둘 수 있습니다. 그림과 해석은 해당 개념 바로 뒤에 둡니다.
- 노트북 첫 부분에 시각화 보조 파일 링크를 둡니다. 공부 중인 코드·수식·검산·실험 설정은 시각화 분리 과정에서 바꾸지 않습니다.
- 수정한 노트북을 각각 새 커널에서 위부터 끝까지 실행하고, 핵심 계산 결과와 실제 그림 출력, 한글 글리프 경고를 확인합니다. 실행하지 않은 결과를 완료로 보고하지 않습니다.
